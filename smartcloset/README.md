## 프로그래밍 요구사항

- Kotlin 2.2.0에서 실행 가능해야 한다.
- Java 코드가 아닌 Kotlin 코드로만 구현해야 한다.
- 프로그램 실행의 시작점은 `Application`의 `main()`이다.
- 프로그래밍 요구 사항에서 달리 명시하지 않는 한 파일, 패키지 등의 이름을 바꾸거나 이동하지 않는다.
- 코틀린 코드 컨벤션을 지키면서 프로그래밍한다.
    - 기본적으로 [Kotlin Style Guide](https://kotlinlang.org/docs/coding-conventions.html)를 원칙으로 한다.

## 프로그래밍 요구사항 2

- indent(인덴트, 들여쓰기) depth를 3이 넘지 않도록 구현한다. 2까지만 허용한다.
- 함수(또는 메서드)가 한 가지 일만 하도록 최대한 작게 만들어라.
- JUnit 5와 AssertJ를 이용하여 정리한 기능 목록이 정상적으로 작동하는지 테스트 코드로 확인한다.

## 프로그래밍 요구사항 3

* 함수(또는 메서드)의 길이가 15라인을 넘어가지 않도록 구현한다.
* 함수(또는 메서드)가 한 가지 일만 잘 하도록 구현한다.
* else를 지양한다.
* 때로는 if/else, when문을 사용하는 것이 더 깔끔해 보일 수 있다. 어느 경우에 쓰는 것이 적절할지 스스로 고민해 본다.
* 힌트: if 조건절에서 값을 return하는 방식으로 구현하면 else를 사용하지 않아도 된다.
* 구현한 기능에 대한 단위 테스트를 작성한다. 단, UI(System.out, System.in, Scanner) 로직은 제외한다.

---

## 구현 기능 목록

### 0. 예외 처리
-  IllegalArgumentException을 발생시키고, "[ERROR]"로 시작하는 에러 메시지를 출력 후 그 부분부터 입력을 다시 받는다.
### 1. 로그인 / 회원가입
* [x] 사용자 정보 입력 받기
    * [x] 이메일 입력
        * 이메일 형식이 아닌 경우 예외 처리
        * 빈 문자열 입력 예외 처리
        * 이메일 중복 체크 (이미 존재하는 경우 예외 처리)
    * [x] 비밀번호 입력
        * 8자 미만, 12자 초과인 경우 예외 처리
        * 영문, 숫자, 특수문자 중 하나라도 없는 경우 예외 처리
        * 빈 문자열 입력 예외 처리
    * [x] 비밀번호 확인 입력
        * 비밀번호와 일치하지 않는 경우 예외 처리
        * 빈 문자열 입력 예외 처리
    * [x] 닉네임 입력
        * 2자 미만, 8자 초과인 경우 예외 처리
        * 빈 문자열 입력 예외 처리
        * 닉네임 중복 체크 (이미 존재하는 경우 예외 처리)

* [x] 회원 정보 저장
    * [x] dto 설계 및 비밀번호 BCrypt 암호화
    * [x] 컨트롤러 구현 및 가입 완료 메시지 출력

### 2. 로그인
* [x] 로그인 정보 입력 받기
    * [x] 이메일 입력
        * 빈 문자열 입력 예외 처리
    * [x] 비밀번호 입력
        * 빈 문자열 입력 예외 처리

* [x] 인증 처리
    * [x] 이메일로 사용자 조회
        * 존재하지 않는 이메일인 경우 예외 처리
    * [x] 비밀번호 검증
        * 비밀번호 불일치 시 예외 처리

* [x] 세션 생성 및 저장
    * [x] 세션에 사용자 ID 저장
    * [x] 로그인 성공 시 사용자 정보 반환

---

## 3. 로그아웃
* [x] 로그아웃 처리
    * [x] 세션 무효화
    * [x] 로그아웃 성공 메시지 반환

---

## 4. 회원탈퇴
* [x] 회원 정보 삭제
    * [x] 로그인 상태 확인 (세션 검증)
        * 세션이 없는 경우 예외 처리
    * [x] 회원 정보 삭제 처리
        * DB에서 사용자 정보 삭제 (CASCADE로 게시글/댓글 전부 삭제)
    * [x] 세션 무효화
    * [x] 회원탈퇴 완료 메시지 반환

---

## 5. 세션 검증
* [x] 세션 검증 (게시판 등 보호된 기능 접근 시)
    * [x] 세션 존재 여부 확인
        * 세션이 없는 경우 예외 처리
    * [x] 세션에서 사용자 ID 추출
---
## 6. 커뮤니티 게시글

* [x] 게시글 작성 기능
    * Board 엔티티 설계 (제목, 내용, 날씨, 이미지 URL, 태그, 작성자, 생성일시)
    * 제목: 필수, 20자 이하 검증
    * 내용: 필수, 100자 이하 검증
    * 날씨: `WeatherType` enum 값 중 하나 선택 (예: SUNNY, CLOUDY, RAINY, SNOWY, WINDY)
    * 이미지: 선택, 최대 1장, S3 업로드 URL 저장
    * 태그: 선택, GPT 추천 결과 기반, 최대 3개까지 저장

* [x] 게시글 목록 조회 (무한 스크롤) 기능
    * `lastId`, `size` 파라미터 사용
    * `lastId`가 없으면 최신 게시글부터 조회
    * `lastId`가 있으면 해당 ID 이전의 게시글을 최신순으로 조회
    * 정렬 기준: 최신순만 사용

* [x] 게시글 상세 조회 기능
    * 게시글 ID로 단건 조회
    * 제목, 내용, 작성자, 날씨, 이미지 URL, 태그, 생성일시 반환

* [x] 게시글 수정 기능
    * 작성자만 수정 가능 (세션의 사용자 ID와 게시글 작성자 ID 비교)
    * 제목, 내용, 날씨, 이미지 URL, 태그 수정 처리

* [ ] 게시글 삭제 기능
    * 작성자만 삭제 가능 (세션의 사용자 ID와 게시글 작성자 ID 비교)
    * 게시글 삭제 시 연관 댓글 CASCADE 삭제 처리


## 7. 댓글

* [ ] 댓글 작성 기능
    * 게시글 ID 지정
    * 내용: 필수, 100자 이하 검증
    * 세션의 사용자 ID를 댓글 작성자로 저장

* [ ] 댓글 조회 기능
    * 게시글 ID 기준으로 댓글 목록 조회
    * 댓글 내용, 작성자, 생성일시 반환

* [ ] 댓글 수정 기능
    * 작성자만 수정 가능 (세션의 사용자 ID와 댓글 작성자 ID 비교)

* [ ] 댓글 삭제 기능
    * 작성자만 삭제 가능
    * 삭제 성공 시 메시지 반환


## 클린 코드 체크리스트

- [ ] 한 메서드에 오직 한 단계의 들여쓰기(indent)만 허용했는가?
- [ ] else 예약어를 쓰지 않았는가?
- [ ] 모든 원시값과 문자열을 포장했는가?
- [ ] 콜렉션에 대해 일급 콜렉션을 적용했는가?
- [ ] 3개 이상의 인스턴스 변수를 가진 클래스를 구현하지 않았는가?
- [ ] getter / setter 없이 구현했는가?
- [ ] 메소드의 인자 수를 제한했는가?
- [ ] 코드 한 줄에 점(.)을 하나만 허용했는가?
- [ ] 메서드가 한 가지 일만 담당하도록 구현했는가?
- [ ] 클래스를 작게 유지하기 위해 노력했는가?